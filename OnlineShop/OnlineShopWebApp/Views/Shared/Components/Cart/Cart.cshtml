@model decimal

<a asp-area="" asp-controller="Cart" asp-action="Index" class="btn p-1">
    <i class="fa-solid fa-cart-shopping icons"></i>
    @if (Model > 0)
    {
        <span class="badge text-bg-primary cart-badge bg-danger rounded-circle">
                @Model
        </span>
    }
</a>